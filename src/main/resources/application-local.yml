spring:
  config:
    activate:
      on-profile: local
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/local_kustaurant?serverTimezone=UTC
    username: root
    password: 1234

logging:
  level:
    com.kustaurant: trace
