name: Github-actions 테스트

on:
  push:
    branches:
      - refactor/github-actions

jobs:
  My-Deploy-Job:
    runs-on: ubuntu-latest

    steps:
      - name: Hi
        run: echo "Hi"
      - name : this is two
        run: echo "mihawk is best player"